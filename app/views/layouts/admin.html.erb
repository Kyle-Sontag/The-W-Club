<!DOCTYPE html>
<html>
  <head>
    <title>Admin - The W Club</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  </head>

  <body>
    <nav class="navbar navbar-dark bg-dark mb-4">
      <div class="container-fluid">
        <span class="navbar-brand">The W Club Admin</span>
        <div>
          <%= link_to "Products", admin_products_path, class: "btn btn-sm btn-outline-light me-2" %>
          <%= link_to "Categories", admin_categories_path, class: "btn btn-sm btn-outline-light me-2" %>
          <%= link_to "View Site", root_path, class: "btn btn-sm btn-outline-light" %>
        </div>
      </div>
    </nav>

    <div class="container">
      <% if notice %>
        <div class="alert alert-success"><%= notice %></div>
      <% end %>

      <%= yield %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>